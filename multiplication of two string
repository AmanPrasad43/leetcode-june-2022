//BigInteger class is used for multiplication of two intgers tht are very large
class Solution
{
    public String multiplyStrings(String s1,String s2)
    {
        //code here.
        BigInteger n1 = new BigInteger(s1);
        BigInteger n2 = new BigInteger(s2);
        BigInteger result; 
        result = n1.multiply(n2);
        String aman = String.valueOf(result);
        return aman;
    }
}
